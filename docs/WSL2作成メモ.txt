
wsl --install
# ダウンロード中: Ubuntu
# インストール中: Ubuntu
# ディストリビューションが正常にインストールされました。'wsl.exe -d Ubuntu' を使用して起動できます

wsl.exe -d Ubuntu
Provisioning the new WSL instance Ubuntu
This might take a while...
Create a default Unix user account: ubuntu
New password:
Retype new password:
passwd: password updated successfully

sudo visudo

%sudo   ALL=(ALL:ALL) ALL
ubuntu ALL=(ALL) NOPASSWD: ALL

Ctrl + O
Crtl + X

quit

wsl.exe -d Ubuntu
cd ~

sudo apt-get -y install zip

# AWS CLI の最新バージョンのインストールまたは更新
# see https://docs.aws.amazon.com/ja_jp/cli/latest/userguide/getting-started-install.html
curl "https://awscli.amazonaws.com/awscli-exe-linux-x86_64.zip" -o "awscliv2.zip"
unzip awscliv2.zip
sudo ./aws/install

aws --version
aws-cli/2.34.37 Python/3.14.4 Linux/5.15.167.4-microsoft-standard-WSL2 exe/x86_64.ubuntu.24

curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.40.4/install.sh | bash

source ~/.bashrc

nvm --version

nvm install 22
nvm use 22


sudo apt update
sudo apt upgrade -y

sudo apt install -y apt-transport-https ca-certificates curl software-properties-common

curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo gpg --dearmor -o /usr/share/keyrings/docker-archive-keyring.gpg
echo "deb [arch=$(dpkg --print-architecture) signed-by=/usr/share/keyrings/docker-archive-keyring.gpg] https://download.docker.com/linux/ubuntu $(lsb_release -cs) stable" | sudo tee /etc/apt/sources.list.d/docker.list > /dev/null

sudo apt update
sudo apt install -y docker-ce docker-ce-cli containerd.io

sudo usermod -aG docker $USER

newgrp docker

docker run hello-world

sudo systemctl enable docker

sudo apt update && sudo apt install -y \
  git curl unzip ca-certificates \
  build-essential python3

mkdir ~/homepage
cd ~/homepage
curl -L https://github.com/okamoto53515606/homepage/archive/refs/heads/main.tar.gz | tar -xz --strip-components=1
rm -rf  migration_project_v1_to_v2  docs prompt_history
cp env_template.txt .env

# チェックコマンド(WSL)
echo "=== versions ===" && \
node -v && npm -v && \
aws --version && \
docker --version && docker info >/dev/null 2>&1 && echo "docker daemon: OK" || echo "docker daemon: NG" ; \
git --version && \
make --version | head -1 && \
gcc --version | head -1 && \
python3 --version && \
curl --version | head -1 && \
unzip -v | head -1 && \
echo "=== AWS auth (.env) ===" && \
( cd /home/ubuntu/homepage && \
  set -a && [ -f .env ] && . ./.env; set +a; \
  aws sts get-caller-identity )

# wslイメージ作成(Windows)
d:
cd .\wsl_backup\
wsl --export Ubuntu homepage-v2-latest.tar

# wslイメージ圧縮(WSL)
cd /mnt/d/wsl_backup
gzip -9 -k homepage-v2-latest.tar
# gz に対してハッシュを取る（配布するのは .gz のほうなので）
sha256sum homepage-v2-latest.tar.gz > homepage-v2-latest.tar.gz.sha256
ls -l --si

# ghコマンド
d:
cd .\homepage-v2-installer\
winget install --id GitHub.cli -e --accept-package-agreements --accept-source-agreements
gh auth login

# githubリリース作成(Windows)
d:
cd .\homepage-v2-installer\
gh release create v2.0.0 `
   D:\wsl_backup\homepage-v2-latest.tar.gz `
   D:\wsl_backup\homepage-v2-latest.tar.gz.sha256 `
   --repo okamoto53515606/homepage `
   --title "v2.0.0" `
   --notes "homepage-v2をリリースしました！"
